####---------- Progress Bar Styling Effects ----------####
# Documentation: https://github.com/Victoria-3-Modding-Co-op/Community-Mod-Framework/wiki/Stylized-Journal-Progress-Bars

# Basic setup for progress bar styling on the current journal
# This effect has to be run inside a scope where 'scope:journal_entry' is available!
# Parameters:
# - progress_bar = name localization key of progress bar
# - index = 0-indexed position of the given progress bar in the journal
# THIS DOES NOT REPLACE THE VANILLA SETUP
REPLACE_OR_CREATE:initialize_com_progress_bar = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { exists = scope:journal_entry }
			}
			error_log = "CMF: No journal_entry scope defined! This effect should be run in the immediate block of a journal entry!"
		}
		else = {
			if = {
				limit = {
					NOT = { container_exists = $progress_bar$ }
				}
				create_container = {
					name = $progress_bar$
					tags = { com_progress_bar }
					parent = scope:journal_entry
				}
			}
			fix_variable_error = { variable = com_progress_bar_$index$ }
			fix_variable_error = { variable = $progress_bar$ }
			fix_variable_error = { variable = com_right_color }
			fix_variable_error = { variable = com_progress_bar_index }
			# Set progress bar
			scope:journal_entry = {
				set_variable = {
					name = com_progress_bar_$index$
					value = container:$progress_bar$
				}
			}
			# save id
			container:$progress_bar$ = {
				set_variable = {
					name = com_right_color
					value = flag:com_transparent_bar
				}
				set_variable = {
					name = com_progress_bar_index
					value = $index$
				}
			}
		}
	}
}

# Basic setup for progress bar styling on the current journal
# This effect has to be run inside a scope where 'scope:journal_entry' is available!
# Parameters:
# - progress_bar = name localization key of progress bar
# - index = 0-indexed position of the given progress bar in the journal
# - color = color of base bar
# THIS DOES NOT REPLACE THE VANILLA SETUP
REPLACE_OR_CREATE:create_com_progress_bar = {
	initialize_com_progress_bar = {
		progress_bar = $progress_bar$
		index = $index$
	}
	set_com_progress_bar_color = {
		progress_bar = $progress_bar$
		color = $color$
		side = left
	}
}

# General setup for progress bar styling on the current journal
# This effect has to be run inside a scope where 'scope:journal_entry' is available!
# Parameters:
# - progress_bar = name localization key of progress bar
# - index = 0-indexed position of the given progress bar in the journal
# - color = color of base bar
# - bar_increase_color = color of drift bar when increasing
# - bar_decrease_color = color of drift bar when decreasing
# - drift_value = a 0-1 script value which sets the size and direction of the drift 
# THIS DOES NOT REPLACE THE VANILLA SETUP
REPLACE_OR_CREATE:create_com_progress_bar_with_drift = {
	create_com_progress_bar = {
		progress_bar = $progress_bar$
		index = $index$
		color = $color$
	}
    set_com_progress_bar_drift = {
        progress_bar = $progress_bar$
        bar_increase_color = $bar_increase_color$
        bar_decrease_color = $bar_decrease_color$
        drift_value = $drift_value$
    }
}

# General setup for progress bar styling on the current journal
# This effect has to be run inside a scope where 'scope:journal_entry' is available!
# Parameters:
# - progress_bar = name localization key of progress bar
# - index = 0-indexed position of the given progress bar in the journal
# - color = color of base bar
# - target_type = icon type for target value
# - target_value = a 0-1 script value which sets the position of the target
# THIS DOES NOT REPLACE THE VANILLA SETUP
REPLACE_OR_CREATE:create_com_progress_bar_with_target = {
	create_com_progress_bar = {
		progress_bar = $progress_bar$
		index = $index$
		color = $color$
	}
	set_com_progress_bar_target = {
		progress_bar = $progress_bar$
		target_type = $target_type$
		target_value = $target_value$
	}
}

# Full setup for progress bar styling on the current journal
# This effect has to be run inside a scope where 'scope:journal_entry' is available!
# Parameters:
# - progress_bar = name localization key of progress bar
# - index = 0-indexed position of the given progress bar in the journal
# - color = color of base bar
# - bar_increase_color = color of drift bar when increasing
# - bar_decrease_color = color of drift bar when decreasing
# - drift_value = a 0-1 script value which sets the size and direction of the drift 
# - target_type = icon type for target value
# - target_value = a 0-1 script value which sets the position of the target
# THIS DOES NOT REPLACE THE VANILLA SETUP
REPLACE_OR_CREATE:create_com_progress_bar_with_both = {
	create_com_progress_bar = {
		progress_bar = $progress_bar$
		index = $index$
		color = $color$
	}
	set_com_progress_bar_drift = {
		progress_bar = $progress_bar$
		bar_increase_color = $bar_increase_color$
		bar_decrease_color = $bar_decrease_color$
		drift_value = $drift_value$
	}
	set_com_progress_bar_target = {
		progress_bar = $progress_bar$
		target_type = $target_type$
		target_value = $target_value$
	}
}

# This effect should be run when the journal entry concludes
# Parameters:
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:remove_com_progress_bar = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = "CMF: Progress bar could not be found"
		}
		else = {
			# Remove progress bar from cache
			remove_list_variable = {
				name = com_progress_bar_cache
				target = container:$progress_bar$
			}

			# Destroy progress bar
			container:$progress_bar$ = { destroy_container = yes }
		}
	}
}

# Sets the color of progress bar
# - progress_bar = name localization key of progress bar
# - color = color of bar
# - side = 'left' or 'right'
REPLACE_OR_CREATE:set_com_progress_bar_color = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			fix_variable_error = { variable = com_$side$_color }
			fix_variable_error = { variable = com_$color$_bar }
			container:$progress_bar$ = {
				set_variable = {
					name = com_$side$_color
					value = flag:com_$color$_bar
				}
				add_tag = cpb_$side$_color_set
			}
		}
	}
}

# Unsets the color of progress bar
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:remove_com_progress_bar_color = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			container:$progress_bar$ = {
				remove_variable = com_left_color
				remove_variable = com_right_color
				remove_tag = cpb_left_color_set
				remove_tag = cpb_right_color_set
			}
		}
	}
}
# Sets the drift colors and the value to be used
# - progress_bar = name localization key of progress bar
# - bar_increase_color = color of drift bar when increasing
# - bar_decrease_color = color of drift bar when decreasing
# - drift_value = a 0-1 script value which sets the size and direction of the drift 
REPLACE_OR_CREATE:set_com_progress_bar_drift = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			fix_variable_error = { variable = com_bar_increase_color }
			fix_variable_error = { variable = com_bar_decrease_color }
			fix_variable_error = { variable = com_drift_value }
			fix_variable_error = { variable = com_$bar_increase_color$_bar }
			fix_variable_error = { variable = com_$bar_decrease_color$_bar }
			fix_variable_error = { variable = $drift_value$ }
			container:$progress_bar$ = {
				set_variable = {
					name = com_bar_increase_color
					value = flag:com_$bar_increase_color$_bar
				}
				set_variable = {
					name = com_bar_decrease_color
					value = flag:com_$bar_decrease_color$_bar
				}
				set_variable = {
					name = com_drift_value
					value = flag:$drift_value$
				}
				add_tag = cpb_drift_set
			}
		}
	}
}

# Removes the progress bar drift effect
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:remove_com_progress_bar_drift = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			container:$progress_bar$ = {
				remove_variable = com_bar_increase_color
				remove_variable = com_bar_decrease_color
				remove_variable = com_drift_value
				remove_tag = cpb_drift_set
			}
		}
	}
}

# Sets the target icon/style and the value to be used
# - progress_bar = name localization key of progress bar
# - target_type = icon type for target value
# - target_value = a 0-1 script value which sets the position of the target
REPLACE_OR_CREATE:set_com_progress_bar_target = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			fix_variable_error = { variable = com_target_type }
			fix_variable_error = { variable = com_target_value }
			fix_variable_error = { variable = com_target_$target_type$ }
			fix_variable_error = { variable = $target_value$ }
			container:$progress_bar$ = {
				set_variable = {
					name = com_target_type
					value = flag:com_target_$target_type$
				}
				set_variable = {
					name = com_target_value
					value = flag:$target_value$
				}
				add_tag = cpb_target_set
			}
		}
	}
}

# Sets the target custom icon
# - progress_bar = name localization key of progress bar
# - target_icon = icon name as flag which points to a dummy ideology
REPLACE_OR_CREATE:set_com_progress_bar_target_icon = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			fix_variable_error = { variable = com_target_type }
			fix_variable_error = { variable = com_target_icon_name }
			fix_variable_error = { variable = com_target_custom_icon }
			fix_variable_error = { variable = $target_icon$ }
			container:$progress_bar$ = {
				set_variable = {
					name = com_target_type
					value = flag:com_target_custom_icon
				}
				set_variable = {
					name = com_target_icon_name
					value = flag:$target_icon$
				}
			}
		}
	}

}
# Removes the progress bar target
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:remove_com_progress_bar_target = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			container:$progress_bar$ = {
				remove_variable = com_target_type
				remove_variable = com_target_value
				remove_tag = cpb_target_set
			}
		}
	}
}

# Sets the number of levels for the progress bar
# - progress_bar = name localization key of progress bar
# - levels = number of levels/parts to the progress bar, evenly spaced
REPLACE_OR_CREATE:set_com_progress_bar_levels = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			fix_variable_error = { variable = com_progress_bar_levels }
			container:$progress_bar$ = {
				set_variable = {
					name = com_progress_bar_levels
					value = $levels$
				}
				add_tag = cpb_levels_set
			}
		}
	}
}

# Removes the levels for the progress bar
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:remove_com_progress_bar_levels = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			container:$progress_bar$ = {
				remove_variable = com_progress_bar_levels
				remove_tag = cpb_levels_set
			}
		}
	}
}

# Sets level highlight for the progress bar
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:set_com_progress_bar_level_highlight = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			fix_variable_error = { variable = com_progress_bar_level_highlight }
			container:$progress_bar$ = {
				set_variable = com_progress_bar_level_highlight
				add_tag = cpb_highlight_set
			}
		}
	}
}

# Removes level highlight for the progress bar
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:remove_com_progress_bar_level_highlight = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			container:$progress_bar$ = {
				remove_variable = com_progress_bar_level_highlight
				remove_tag = cpb_highlight_set
			}
		}
	}
}

# Hides the progress bar, so it is no longer shown
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:hide_com_progress_bar = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			fix_variable_error = { variable = com_hide_progress_bar }
			container:$progress_bar$ = {
				set_variable = com_hide_progress_bar
				add_tag = cpb_hidden_set
			}
		}
	}
}

# Unhides the progress bar
# - progress_bar = name localization key of progress bar
REPLACE_OR_CREATE:unhide_com_progress_bar = {
	hidden_effect = {
		if = {
			limit = {
				NOT = { container_exists = $progress_bar$ }
			}
			error_log = error_no_progress_bar_scope
		}
		else = {
			container:$progress_bar$ = {
				remove_variable = com_hide_progress_bar
				remove_tag = cpb_hidden_set
			}
		}
	}
}

# A wrapper effect to easily run an effect inside 'scope:$progress_bar$'
# - progress_bar = name localization key of progress bar
# - effect = effect or series of effects to be run inside 'scope:$progress_bar$'
REPLACE_OR_CREATE:run_in_com_progress_bar_scope = {
	container:$progress_bar$ ?= {
		$effect$
	}
}

# Valid bar and drift colors:
## Blue - blue/default
## Red - red/bad
## Green - green
## Gold - gold
## White - white

# Valid target types:
## Journal icon - journal_icon
## Gold marker icon - gold_marker
## Gold divider bar - gold_bar
## Red divider line - red_line
## Blue divider line - blue_line
## Green divider line - green_line
## White divider line - white_line
## Gold divider line - gold_line

REPLACE_OR_CREATE:com_debug_progress_bar = {
	set_variable = {
		name = com_index
		value = je:$je$.var:com_progress_bar_$index$
	}
	set_variable = {
		name = com_id
		value = container:$progress_bar$.var:com_id
	}
	set_variable = {
		name = com_left_color
		value = container:$progress_bar$.var:com_left_color
	}
	set_variable = {
		name = com_right_color
		value = container:$progress_bar$.var:com_right_color
	}
	set_variable = {
		name = com_bar_increase_color
		value = container:$progress_bar$.var:com_bar_increase_color
	}
	set_variable = {
		name = com_bar_decrease_color
		value = container:$progress_bar$.var:com_bar_decrease_color
	}
	set_variable = {
		name = com_drift_value
		value = container:$progress_bar$.var:com_drift_value
	}
	set_variable = {
		name = com_target_type
		value = container:$progress_bar$.var:com_target_type
	}
	set_variable = {
		name = com_target_value
		value = container:$progress_bar$.var:com_target_value
	}
}