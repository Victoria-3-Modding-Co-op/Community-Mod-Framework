macro = {
    description = "Get situation struct scope"
    definition = "ComSituation"
    replace_with = "JournalEntry.MakeScope.Var('com_situation')"
}

macro = {
    description = "Get left side struct scope"
    definition = "ComSituationLeft"
    replace_with = "ComSituation.Var('com_side_left')"
}

macro = {
    description = "Get right side struct scope"
    definition = "ComSituationRight"
    replace_with = "ComSituation.Var('com_side_right')"
}

macro = {
    description = "Overwrites portrait value if set, or uses specified default"
    definition = "ComSelectPortraitVar(Variable,Default)"
    replace_with = "SelectLocalization( Character.MakeScope.Var(Variable).IsSet, Character.MakeScope.Var(Variable).GetFlagName, Default )"
}

macro = {
    description = "Selects situation ending conditions if set specifically"
    definition = "ComVictoryCondition(Type)"
    replace_with = "SelectLocalization(Scope.Var('com_victory_condition').IsSet,Scope.Var('com_victory_condition').GetFlagName,Type)"
}

macro = {
    description = "Selects situation ending effects if set specifically"
    definition = "ComVictoryEffect(Type)"
    replace_with = "SelectLocalization(Scope.Var('com_victory_effect').IsSet,Scope.Var('com_victory_effect').GetFlagName,Type)"
}

macro = {
    description = "Set trigger checkbox"
    definition = "ComTriggerBox(Boolean)"
    replace_with = "SelectLocalization(Boolean,'com_green_checkmark_box','com_simple_box' )"
}

macro = {
    description = "Logical NAND"
    definition = "Nand(A,B)"
    replace_with = "Not(And(A,B))"
}

macro = {
    description = "Logical NOR"
    definition = "Nor(A,B)"
    replace_with = "Not(Or(A,B))"
}

macro = {
    description = "Logical XOR"
    definition = "Xor(A,B)"
    replace_with = "And(Or(A,B),Nand(A,B))"
}